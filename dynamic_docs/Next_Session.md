<!-- Purpose: Claude's handoff note -- what to pick up, open questions, and suggested first steps for the next session. Written by Claude at session end. -->

## Pick up here

- **No specific next feature named at close of this session.** Ask rather than assume a direction.
- **Any Running Order `.xlsx` already generated under the old (buggy) two-`tweaks`-column schema needs regenerating** -- the schema fix only affects newly-generated files. Worth checking with the user whether they still have old ones in circulation before this is forgotten.
- **`CG_Chart_`/`CG_Link_` shape naming only applies to charts inserted from this point forward.** Slides in already-generated decks won't have named shapes retroactively -- worth being aware of if the Position Finder tool is reported "not finding" an icon's matching chart on an older output.
- **The `target` tweak convention (`key:value^key2:value2`, caret-delimited) is intentionally per-chart, not a system standard** -- if a third chart needs a tweak, don't assume it must follow the same syntax; ask, the same way this session's design conversation went for `column_ci_full`/`line_ci_full`.
- **The 3-sig-fig decimal-place rule (`max(0, 2 - floor(log10(abs(value))))`, driven by the mean of the relevant value set) was scoped deliberately narrowly this session** -- `line_ci_full`'s table and `column_ci_full`'s key/annotation only. If asked to extend it to other charts or the two Base Tables, treat that as a fresh design conversation (which value(s) drive the shared count for that context), not an automatic rollout of the same formula.
- **Font mismatch flagged in an earlier session, still not resolved.** Both CI charts use Calibri (Architecture Decision 27) rather than the Arial the CI report's own spec (`CHART_SPEC.md`) asks for -- flagged to the user as a deliberate exception, never explicitly confirmed either way.
- **The relative-to-chart-corner picture positioning built for the hyperlink icon is a reusable pattern, not yet extracted anywhere.** Position Finder's own chart-matching logic (this session) is a second consumer of the same underlying convention (`CG_Chart_`/`CG_Link_` naming plus the top-right-corner offset), but the maths itself still lives only in `assembly_engine._insert_hyperlink_icon`.
- **An unbuilt idea raised in an earlier session, not touched since:** letting a template textbox sit *in front of* an inserted chart picture rather than behind (current/default) -- confirmed technically buildable (move the picture's XML element in the slide's shape tree) but not built. Would need a decision on default vs per-row opt-in if picked up.
- **The same Excel `cell.value`-without-`number_format` gap fixed for Output Table content (Architecture Decision 34) may still exist in the Stat Tags, Chart Store, and Running Order Excel readers.** Flagged repeatedly now, still not investigated.
- **Base Table trim (from an earlier session) is still an unconfirmed, potential breaking change** for any existing workfile using one of the eight removed styles -- carried forward across several close-downs now.
- **SVG transparent backgrounds for Output Tables** -- still deferred, case-by-case, per the user's own earlier plan.
- **A residual "pixel or two too low" vertical offset in `table_cardtile`'s chart-cell placement** -- still parked, not investigated.
- **A larger, unscoped feature raised in conversation some sessions back: conditional per-unit slide deletion plus a matching index-page regeneration.** Still needs its own dedicated design session. Nothing built yet.

## This session's work, for context

Per the standing rule, no chart-specific visual detail for `column_ci_full`/`line_ci_full` is recorded here beyond what's needed for continuity -- see the code. The session's arc: a hyperlink icon redraw (the two chain-link rects now genuinely overlap, at the user's direct correction); the first working `tweaks` feature on any Base Chart (a `target` reference line, agreed piece by piece -- syntax convention, colour, label wording, legend-vs-on-chart placement); a real bug hunt that traced "I keep losing my tweaks" all the way back to a duplicated `tweaks` column in the Running Order schema; a jointly-designed decimal-formatting rule applied narrowly to two charts; several rounds of legend/annotation polish on `column_ci_full` (drop "(this organisation)", drop the Mean line but keep its legend text, z-order, a translucent background box behind two labels); a COM threading bug fix for `save_pdf` failing on repeat runs; and, closing the session, a new naming convention (`CG_Chart_`/`CG_Link_`) plus a new tool built on top of it (Position Finder) -- both agreed via a short back-and-forth on exactly how stale a `row_id`-keyed name is allowed to go before it stops being useful.

Governed docs (Architecture, Functional Spec, Feature List, Glossary) were updated for every genuinely system-level piece of this (the tweaks-in-practice correction, the two new Decisions, the new tool) at the user's explicit request, immediately before this close-down -- Primer was left untouched as nothing this session changed domain rationale.
