<!-- Purpose: Claude's handoff note -- what to pick up, open questions, and suggested first steps for the next session. Written by Claude at session end. -->

## Pick up here

- **"Project Folder" button (added this session) has not been run against the live app yet.** Worth a first-run check: button sits at the bottom of the sidebar, below Version/Sign Out, disabled with no workfile open, and opens the correct workfile folder in Explorer once one is.
- **Export Picture button (added the prior session) -- confirm still working as expected**, in case anything about it needs revisiting now a second sidebar/Charts-sheet change has landed alongside it.
- **Radar family is done and should be re-tested against real data one more time** (carried forward from before last session) now everything (submission code fix, `outputAvailability` removal, radar transformers themselves) is in place together -- worth confirming both SPs still resolve cleanly end to end, not just individually as each fix landed.
- **The radar-cycling multi-submission-per-organisation limitation is still unverified.** `sp_a_generic_radar_to_dual_bar`'s per-organisation cycling gives every submission under one organisation identical values, since the API is driven by `organisation_id` not `submission_id`. Needs checking against a real multi-submission organisation.
- **16 SPs from an earlier session's full mapping still have no transformer.** That's a substantial chunk of remaining build work across NumericSeries, NumericCompositional, and CategoricalCompositional -- all existing shapes, no new shape code needed.
- **PairedSurveyData still has no transformer, no Base Chart, and no `reference_ids.py` entry for Summary Stat Tags.** Ask which to prioritise rather than assuming.
- **Per-unit `metadata: dict` on the shared `Unit` base class is designed but not coded** -- needed before cross-service transformers (3 of the 26 mapped SPs) can be built.
- **Worth a visual check, not a design task:** with `metric_periods` no longer raising on an unresolvable id, a metric with no data now flows through to whichever Base Chart renders it. Worth looking at how a couple of real charts actually handle that now, since it was previously untestable (the row would fail before rendering).
- The same Excel `cell.value`-without-`number_format` gap fixed for Output Table content (Architecture Decision 34) may still exist in the Stat Tags, Chart Store, and Running Order Excel readers. Flagged repeatedly now, still not investigated.
- Any Running Order `.xlsx` exported before the period-field storage rework should be treated as stale and re-exported, not reused.
- Base Table trim (from an earlier session) is still an unconfirmed, potential breaking change for any existing workfile using one of the eight removed styles -- carried forward across several close-downs now.
- SVG transparent backgrounds for Output Tables -- still deferred, case-by-case, per the user's own earlier plan.
- A residual "pixel or two too low" vertical offset in `table_cardtile`'s chart-cell placement -- still parked, not investigated.
- A larger, unscoped feature raised in conversation some sessions back: conditional per-unit slide deletion plus a matching index-page regeneration. Still needs its own dedicated design session. Nothing built yet.
- `CG_Chart_`/`CG_Link_` shape naming only applies to charts inserted from this point forward -- carried forward, still relevant if the Position Finder tool is reported "not finding" a match on an older output.
- The `target` tweak convention is intentionally per-chart, not a system standard -- carried forward; don't assume a third chart's tweak needs the same syntax without asking.
- The relative-to-chart-corner picture positioning built for the hyperlink icon is a reusable pattern, not yet extracted anywhere -- carried forward, still only living in `assembly_engine._insert_hyperlink_icon`.
- An unbuilt idea from an earlier session: letting a template textbox sit in front of an inserted chart picture rather than behind -- confirmed buildable, not built, carried forward.
- If SVG-text-as-real-text is ever wanted for production (not just testing), font embedding into the exported PDF hasn't been verified -- check with `pdffonts` or Acrobat's Document Properties on an actual export before trusting it, rather than assuming PowerPoint's own font-embedding setting covers it (it doesn't, for text inside an embedded SVG image).

## This session's work, for context

Two threads. First, a genuine small feature: a "Project Folder" button was added to the bottom of the sidebar, opening the current workfile's folder in Explorer. Straightforward, mirrors the pattern already used elsewhere for local-machine assumptions (`run_chartgen.bat`, one user per machine).

Second, an extended exploratory exercise that was fully reverted by the end of the session, at the user's own request -- nothing from it persists in the codebase or governed docs. It started with two genuine questions (how SVG font embedding survives a PowerPoint-to-PDF export; how font sizes scale when an SVG is drawn oversized then shrunk into a smaller picture frame), then moved into deliberately experimental code: two "real SVG text instead of glyph outlines" variants of the CI charts (`line_ci_full2`, `column_ci_full2`), and a "draw everything 4x bigger internally, then physically shrink the inserted picture back down" bodge variant (`line_ci_full3`), the latter requiring a hardcoded, explicitly-flagged-as-ugly special case in `assembly_engine.insert_chart` that broke the "Base Charts are outside the system boundary" standing rule on purpose, for this one test. All three files, their registry/chart_type_map wiring, and the assembly_engine special case were deleted/removed in full once testing was done. Worth knowing this happened, in case any residual questions come up about SVG font behaviour or scaling technique -- the reasoning is in this session's chat, not written up anywhere else, since none of it became a real feature.
</content>
