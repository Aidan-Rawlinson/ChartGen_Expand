<!-- Purpose: Claude's handoff note -- what to pick up, open questions, and suggested first steps for the next session. Written by Claude at session end. -->

## Pick up here

- **No specific next feature named at close of this session.** The SVG rendering rollout (this session's whole build) is complete across every Base Chart and Base Table -- ask rather than assume a direction.
- **Check whether any live workfile holds a Custom Chart or Custom Table saved before this session.** Both insertion call sites (`insert_chart`, `insert_table`) now assume SVG bytes unconditionally, with no raster fallback -- a pre-existing Custom Chart/Table would fail outright on insertion, not degrade gracefully. The only one known to exist is a prior session's one-off `ranked_column` Custom Chart test; worth confirming its status (re-save under the new contract, or delete) before it surprises anyone mid-batch-run.
- **Text in charts/tables is not searchable or selectable in PowerPoint or the exported PDF -- accepted, not an open bug.** Tried both `svg.fonttype` settings (`"path"`, `"none"`); neither exposes text to PowerPoint's own Find or the PDF's text layer, since both treat an embedded SVG as an opaque picture regardless of what's inside it. See Architecture Decision 27 and Current_State's "Known gaps" -- don't re-litigate this from scratch without a genuinely new angle (e.g. a completely different insertion mechanism, not a font/setting tweak).
- **Custom Tables round-trip testing still not done at all** (carried forward from a prior session, unrelated to this one's work) -- built and wired, never taken through a full download/edit/paste-back/save loop.
- **`chart_type_ref` -> `base_chart_name` rename is agreed but deliberately not started** (carried forward). Real breaking change -- needs its own dedicated session. Wait for the user to explicitly ask to start it.
- **Chart-component cells (`{n}`) in Output Tables are parked, not built** (carried forward). Recognised by the grid's own grammar, not resolved or rendered.
- **A manually-created Output Table has no automatic Running Order placement** (carried forward). Only template-extracted (yellow-box) tables get an `insert_table` row for free.

## This session's work, for context

- **Every Base Chart (20) and Base Table (10) now renders as SVG instead of PNG, with Calibri as the standard font.** Came out of a live spike-and-prove cycle: built the SVG insertion mechanism (`svg_insert.py`, the OOXML dual-blip a native SVG picture requires) and proved it against one table type (`table_cardtile`) first, iterating live through several real problems (font substitution causing letter-spacing corruption; live-text mode (`svg.fonttype="none"`) failing to deliver genuine searchability while corrupting PDF character positions; a minor unhinted-vector-text rendering artifact at low zoom in Adobe Reader) before rolling the settled methodology out to the remaining 19 charts and 9 tables in one pass. See Architecture Decision 27 and Current_State for full detail -- worth reading in full before touching any of this again.
  - **Font investigation, if it helps to know the reasoning trail:** the person's first three font choices (Avenir Next LT Pro Light, Poppins, Aptos Narrow) all turned out to be Office-internal fonts (embedded-in-file, or an Office "cloud font") rather than genuinely OS-installed ones matplotlib can see -- none resolved. Arial (confirmed genuinely installed) worked correctly; Calibri was then substituted in as the final choice at the person's request, with no further issues.
  - **`table_cardtile_svg.py` (the spike copy) was folded back into `table_cardtile.py`** once the methodology was settled -- no separate file, no duplicate registry entry, remains anywhere.
  - **Both Preview sandboxes (Charts sheet, Output Tables tab) switched from `st.image()` to `st.markdown()`** for the same reason `add_picture()` couldn't take SVG directly -- `st.image()` also goes through PIL. Each tab carries its own small `_svg_preview_html()` helper (not shared between them), forcing the SVG to the same pixel width the raster path used to compute.
  - **Custom Charts/Custom Tables contract documents updated** to tell an AI-assisted edit to return SVG bytes and set Calibri, and to explicitly warn against `svg.fonttype="none"` given what this session found.
- **PDF export mechanism changed three times this session, before settling back on the simplest version.** Started on `SaveAs(path, 32)` (inherited from a prior session's investigation, Decision 26); tried `ExportAsFixedFormat` with `Intent=2`/`BitmapMissingFonts=False` while chasing the SVG-text-positioning-corruption problem specifically; neither changed the outcome, so at the user's explicit request it was reverted to `ExportAsFixedFormat` with only `FixedFormatType=2` and every other parameter left at default. See Architecture Decision 26 (revised this session) -- note its own re-opened caveat: the raster-downsampling finding it documents was established against the pre-SVG raster pipeline, and hasn't been independently re-confirmed against `insert_picture`'s own raster path since this session's changes, though nothing suggests it would differ.
- **Architecture and Feature List updated in the mirror; re-uploaded by the user this close-down.** Functional Spec, Glossary, Primer, and the Docs Maintenance Guide needed no changes -- checked against this session's changes and found nothing they currently say became false (none specified a raster format).
