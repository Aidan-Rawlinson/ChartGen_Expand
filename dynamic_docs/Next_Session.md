<!-- Purpose: Claude's handoff note -- what to pick up, open questions, and suggested first steps for the next session. Written by Claude at session end. -->

## Pick up here

- **No specific next feature named at close of this session.** The `base_chart_name` rename and the Output Table simplification (this session's whole build) are both complete and tested by the user -- ask rather than assume a direction.
- **Chart-component cells (`{n}`) in Output Tables are parked, not built** (carried forward). Recognised by the grid's own grammar, not resolved or rendered.
- **Custom Tables round-trip testing is no longer an open gap** -- the user confirmed this session it's been tested "to a point" and asked to mark it done. Don't reopen this from scratch without a genuinely new reason.
- **Text/table searchability limitation, PDF raster-downsampling on `insert_picture`, `table_ledger`'s serif override** -- all carried forward unchanged from the SVG rendering session (see Decisions.md/Progression_Log for full detail); nothing this session touched any of them.
- **If a `chart_type_ref` reference ever surfaces again anywhere** (a missed file, a stray doc, a user's saved snippet) -- it should read `base_chart_name`. The rename was confirmed complete via full-codebase search at the time, but this is worth a moment's sanity check if anything chart-type-related misbehaves next session.

## This session's work, for context

- **`chart_type_ref` -> `base_chart_name` rename, executed in full.** Previously agreed in principle and deliberately deferred across at least two prior sessions as a breaking change. This session: mechanical rename across 12 code files plus all four governed docs (Architecture, Functional Spec, Feature List, Glossary); `table_type_ref` (a different, table-side identifier) was correctly left untouched throughout. File version bumped `0.0.3` -> `0.0.4` in `version_compatibility.csv`, with `0.0.3` removed from the readable list -- a pre-rename workfile will not open, no migration path, per the original deferred decision's own framing. No live workfiles existed at the time, confirmed with the user, so this had no practical fallout.
- **Output Table generation simplified, at the user's explicit direction, across three related changes:**
  - **Fixed default size (7 rows x 4 columns) for every Output Table**, whichever route creates it -- `grid_store.DEFAULT_TABLE_ROWS`/`DEFAULT_TABLE_COLUMNS`, no user-configurable Rows/Columns anywhere at creation time.
  - **Yellow box syntax reduced to the literal word `Table`** (case/whitespace-insensitive) -- no name, no Rows/Columns. Discussed and settled the resulting identity question explicitly with the user: since the box carries no name any more, every occurrence (matched or free-floating) now always creates a brand-new Output Table, auto-named `Table_1`, `Table_2`, ... (underscore, per the user's correction on the naming format), never matched against an existing table by name -- re-uploading the same template twice produces two independent sets of tables, and the user re-links Running Order rows to whichever set they want. This removed the old by-name dedup/"already_present" tracking in `import_flow.merge_output_tables_from_template` entirely, and the `output_tables_by_name` lookup mechanism in `generation.py` in favour of `ph.table_id` set directly on the placeholder.
  - **Manually-created tables (Output Tables tab's "+ New Output Table") now get automatic Running Order placement too**, closing what was previously a "known gap" for that creation route specifically. New shared helper `row_ops.append_content_row_above_footer` inserts a fresh row immediately above the first `save_ppt` row. Discussed and settled with the user: no real slide/position is needed at creation time ("that's a user issue not a system issue") -- `insert_table` already errors gracefully per-row at run time if `slide_index` is still blank when a batch runs, so this isn't a new risk. Default sizing: 70% width / 50% height on the page-percentage scale, agreed explicitly with the user.
  - **A `StreamlitAPIException` surfaced during the user's own testing** (writing to `st.session_state["ot_table_choice"]` after that widget's already been instantiated the same run) -- fixed via the same pending-key staging pattern already used elsewhere in `output_tables_tab.py` for identical reasons. Confirmed fixed; a follow-up report of the same symptom turned out to be a second, still-running stale Python process the user had missed, not a code issue.
- **Architecture, Functional Spec, and Feature List updated in the mirror; re-uploaded by the user this close-down.** Glossary needed only the mechanical rename. Primer and the Docs Maintenance Guide needed no changes at all -- checked against this session's changes and found nothing they currently say became false.
