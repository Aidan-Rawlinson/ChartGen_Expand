<!-- Purpose: Claude's handoff note -- what to pick up, open questions, and suggested first steps for the next session. Written by Claude at session end. -->

## Pick up here

- **No specific next feature named at close of this session.** Charts-inside-table-cells (this session's whole build) is working end to end for both Base Table styles. The user's own words: "we'll need to tweak the process, but that's amazing" — expect refinement requests on this feature specifically, not necessarily a brand-new area. Ask rather than assume a direction.
- **Check whether any existing workfile references one of the eight deleted Base Table styles** (`table_brutalist`, `table_editorial`, `table_freeform`, `table_ledger`, `table_pill`, `table_softui`, `table_terminal`, `table_zebra`) before this surfaces as a live bug — an `insert_table` row with one of these as `table_type_ref` will fail at render time (`Unknown table_type_ref`). This was not explicitly checked or confirmed clear with the user this session, unlike the earlier `chart_type_ref` rename where "no live workfiles existed" was actually confirmed. Worth raising proactively next session if it hasn't come up already.
- **A residual "pixel or two too low" vertical offset in `table_cardtile`'s chart-cell placement**, noted by the user as close-but-not-perfect after the tight-bbox fix, explicitly parked ("let's not worry about dealing with that yet"). Likely something in the SVG rasterisation/display step or an EMU-to-pixel rounding point, rather than the core `_resolve_chart_cells` math being wrong — but not investigated. Pick this up if the user raises it again.
- **SVG transparent backgrounds** — discussed and deferred, not built. User's own plan: newer/better charts are coming, transparency to be addressed on those; tables to be handled case-by-case later, not as a blanket change now. `facecolor="none"`/`transparent=True` in each `savefig` call is the mechanism, already discussed and ready to apply whenever asked — figure-level background only, distinct from the cell/card patches' own `facecolor="white"`, which is a separate, bigger design question (a table with no visible cell backing at all) if it's ever wanted.
- **Chart-cell precision work was only done for `plain_grid` and `table_cardtile`.** Any future Base Table style (built-in or, more likely given this session's direction, a Custom Table saved via the bundle route) needs its own worked-through answer to (a) what "the cell" means for that style's own decoration, and (b) whether it needs `plain_grid`'s crop-free approach or `table_cardtile`'s tight-bbox-correction approach — see `custom_tables/contract.py`'s own standing guidance section, which is where this is actually written down for whoever builds one next.
- **Governed docs (Architecture, Functional Spec, Feature List, Glossary) were updated and confirmed this session but not yet re-uploaded to Claude Desktop Project Files as of this close-down** — first thing to prompt for/verify next session if it wasn't done immediately after.

## This session's work, for context

This was one of the largest sessions to date — built, in order: NHS chart-title placeholder replacement (reverse-engineered from the user's old VBA); the Chart Store feature from scratch (storage, Charts sheet UI, Excel round-trip); a `T`/`C` id-prefix scheme so Stat Tags and Chart Store entries can coexist in the same table cell; a full EMU-only unit consolidation across every Base Chart and Base Table (removing the old percent-of-a-fixed-reference conversion entirely); the actual charts-inside-table-cells feature (marker syntax, `(image_bytes, chart_cells)` return contract, layered-PPTX-picture insertion for the final report, percent-based nested-SVG splice for Preview); a trim of Base Tables from ten styles down to two while working through exactly what "the cell" means and how to report its position precisely for each of the two kept; and a full pass over all four governed docs to bring them current. See Current_State.md for the full breakdown of what was built and what bugs were found/fixed along the way, and Decisions.md for the reasoning behind the harder design calls (particularly the back-and-forth on chart-cell precision, which took several rounds of the user correcting an initially-wrong diagnosis before landing on the right fix).

**The user was directly, usefully critical at several points this session** — twice on cell-position precision specifically (first correctly rejecting a fix that improved things without addressing the actual reported-position inaccuracy, then rejecting a second, content-bleed-based explanation as not matching what they'd actually observed) — both times this led to a better, more rigorous fix (the tight-bbox correction, verified empirically rather than assumed) than the first attempt would have produced. Worth remembering this pattern: when the user says a fix is "close but not quite right" or pushes back on a diagnosis, that's a strong signal to re-derive from first principles and verify empirically rather than patch the existing explanation.
