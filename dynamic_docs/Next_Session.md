<!-- Purpose: Claude's handoff note -- what to pick up, open questions, and suggested first steps for the next session. Written by Claude at session end. -->

## Pick up here

- **No specific next feature named at close of this session.** This session was almost entirely new Base Chart/Base Table work (`sparkline1`, `celltest`, `line_has_data`, the `line_ci_*` family, `ci_grid`, `ci_cardtile`) plus four core-system fixes. Expect either continued refinement requests on the new charts/tables specifically, or a genuinely new area -- ask rather than assume a direction.
- **Standing rule, confirmed explicitly this session and now in `Current_State.md`'s own read-first block: Base Charts and Base Tables (their existence, their internals) are never documented in the governed docs.** Only a genuine core-system-level change belongs there. Don't relitigate this without the user raising it again, and don't let a future close-down accidentally log a new chart/table's existence "for completeness."
- **The same Excel `cell.value`-without-`number_format` gap fixed this session for Output Table content (Architecture Decision 34) may exist in the Stat Tags, Chart Store, and Running Order Excel readers too.** Flagged, not investigated. Worth checking proactively, or picking up if the user reports the same "5% became 0.05" symptom somewhere else.
- **Base Table trim (from an earlier session) is still an unconfirmed, potential breaking change** for any existing workfile using one of the eight removed styles (`table_brutalist`, `table_editorial`, `table_freeform`, `table_ledger`, `table_pill`, `table_softui`, `table_terminal`, `table_zebra`) -- carried forward twice now, still not checked or confirmed clear with the user.
- **SVG transparent backgrounds for Output Tables** -- still deferred, case-by-case, per the user's own earlier plan. Not raised again this session; the chart side of this is effectively done now (every chart built this session already renders with a transparent background from the outset), but no table background code has changed.
- **A residual "pixel or two too low" vertical offset in `table_cardtile`'s chart-cell placement** -- carried forward from an earlier session, still parked, still not investigated.
- **Governed docs are current and re-uploaded** -- nothing outstanding here going into next session, unlike the last two close-downs.

## This session's work, for context

Built five new Base Charts (`sparkline1`, `celltest`, `line_has_data`, `line_ci_median`/`line_ci_2`/`line_ci_5pct`/`line_ci_0_5pct`) and two new Base Tables (`ci_grid`, `ci_cardtile`), through many rounds of live-tested visual and behavioural refinement -- none of this is in the governed docs, per the standing rule above; see `Current_State.md` for a compressed account of what each one does, kept there rather than in the static docs specifically because it's Base Chart/Table content. Alongside that, found and fixed four genuine core-system bugs, all now in Architecture Decisions 31-34: Chart Store population inheritance not applying inside table cells; Chart Store id issuance able to collide when its counter fell behind; no way to represent a line break in Output Table content (plus a related font-fit gap that would have hidden a second line even once one existed); and Excel-typed percentages being silently read back as a raw decimal instead of the percentage text. See `Decisions.md` for the reasoning behind the two more involved diagnoses (the sparkline `None`-handling fix, and the Chart Store id-collision root cause).

**The user corrected me plainly and usefully at least twice this session on real technical errors** -- once when an over-corrected fix for a genuine crash silently suppressed an entire, mostly-complete data line rather than drawing a real gap at just the missing point ("that isn't a fix, that is the inducement of a whole new mass of errors"), and once when I mislabelled duplicating logic across two standalone files as a "shared helper," which read as a boundary violation given Base Charts/Tables must never import from each other or anywhere else. Both times the correction was specific and the fix that followed was the right one. Worth remembering the general pattern from earlier sessions still holds: a direct correction here is a strong, reliable signal to re-derive from first principles rather than patch the existing explanation.
