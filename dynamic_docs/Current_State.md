<!-- Purpose: A snapshot of where the project stands right now -- what works, what is in progress, what is broken. Rewritten by Claude each session. -->

## Status: New Project

code_base/ChartGen contains starter/seed files, but this is still a new project -- code_base being non-empty doesn't change that. Structure matches the Architecture document's Software domain layout: `app.py`, `run_chartgen.bat`, `requirements.txt`, `user_resources/`, `.streamlit/`, and `core/` with its six sub-packages (`session_shell/`, `workfile/`, `acquisition/`, `output_generation/`, `shared/`, `ui/`).

No in-progress work, no known issues logged yet.
